import { Routes } from '@angular/router';
import { DashboardSubscriptionComponent } from './dashboard-subscription.component';

export const DASHBOARD_SUBSCRIPTION_ROUTES: Routes = [
    {
        path: '',
        component: DashboardSubscriptionComponent
    }
];