import { Routes } from '@angular/router';
import { HomeDetailsComponent } from './home-details.component';

export const DASHBOARD_HOME_DETAILS_ROUTES: Routes = [
    {
        path: '',
        component: HomeDetailsComponent
    }
];