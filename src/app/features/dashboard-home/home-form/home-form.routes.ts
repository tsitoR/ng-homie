import { Routes } from '@angular/router';
import { HomeFormComponent } from './home-form.component';

export const DASHBOARD_HOME_FORM_ROUTES: Routes = [
    {
        path: '',
        component: HomeFormComponent
    }
];